= form_for @user do |f|
  - if @user.errors.any?
    #ErrorModule
      %h2= "#{pluralize @user.errors.count, 'Error'} Prevented This User From Being Created"
      %ol
        - @user.errors.full_messages.each do |error|
          %li= error
  = render f
  %p= f.submit 'Create User'


%table
  %tr
    %th Name
    %th Email
    %th Customer
    %th Sites
    %th Admin
    
  - @users.each do |user|
    %tr
      %td= link_to user.name, user
      %td= user.email
      %td= user.customer ? link_to(user.customer.name, customer_path(user.customer)) : 'n/a'
      %td= number_with_delimiter user.sites.count
      %td= user.admin? ? 'Yes' : 'No'
      