admin:
  customer: active
  name: admin
  password_digest: <%= BCrypt::Password.create(common_fixture_password) %>
  admin: true

active_admin:
  customer: active
  name: activeAdmin
  password_digest: <%= BCrypt::Password.create(common_fixture_password) %>
  admin: true

cancelled_admin:
  customer: cancelled
  name: cancelledAdmin
  password_digest: <%= BCrypt::Password.create(common_fixture_password) %>
  admin: true

different_admin:
  customer: different
  name: differentAdmin
  password_digest: <%= BCrypt::Password.create(common_fixture_password) %>
  admin: true

active_user:
  customer: active
  name: activeUser
  password_digest: <%= BCrypt::Password.create(common_fixture_password) %>
  admin: false

cancelled_user:
  customer: cancelled
  name: cancelledUser
  password_digest: <%= BCrypt::Password.create(common_fixture_password) %>
  admin: false